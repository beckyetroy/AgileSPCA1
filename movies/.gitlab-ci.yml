image: node:latest

# Pipeline
stages:
  - cd movies
  - install
  - build

# Jobs 
install_dependencies:
  stage: install
  script:
    - cd movies
    - npm ci
  artifacts:
    paths:
      - node_modules/

bundle_app:
  stage: build
  script:
    - cd movies
    - npm run build